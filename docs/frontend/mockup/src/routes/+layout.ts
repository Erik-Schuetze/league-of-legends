// The review build is fully static, exactly like the design requires of the
// real frontend: no server, no runtime data fetching.
export const prerender = true;
export const trailingSlash = "always";
